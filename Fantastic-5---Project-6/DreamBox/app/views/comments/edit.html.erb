<div class="container">  
	<h1>Edit your comment</h1>
	<%= image_tag @comment.image.url(:medium)%>
	<%= simple_form_for([@post, @comment]) do |f|%>
	  <%= f.input :comment %>
  		<%= f.submit %>
  		<%end%>
</div>